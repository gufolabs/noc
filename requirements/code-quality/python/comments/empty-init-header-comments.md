## Empty Python package initializers must not contain header comments

### Scope

Python files named `__init__.py` that do not contain executable code.

### Requirement

Empty package initializer files **MUST NOT** contain standard file header comments.

## Why?

An empty file must remain empty.
