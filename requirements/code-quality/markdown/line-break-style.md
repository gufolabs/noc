# Line Break Style

## Scope

All Markdown files.

## Requirement

Manual line breaks **MUST NOT** be used for paragraph formatting.

The `<br>` tag **MUST NOT** be used except inside table cells where an explicit line break is required.

## Why?

Markdown source should represent document structure rather than manual visual formatting.

Restricting line breaks prevents inconsistent rendering across Markdown processors.