# Table Style

## Scope

All Markdown files containing tables.

## Requirement

Tables **MUST** be used only for structured data.

Tables **MUST NOT** be used for visual layout.

Tables **MUST** contain a header row.

## Why?

Tables should represent structured relationships between values and not replace document formatting.