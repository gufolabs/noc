# Raw HTML Usage

## Scope

All Markdown files.

## Requirement

Raw HTML **MUST NOT** be used unless the required structure cannot be represented using Markdown syntax.

## Why?

Avoiding unnecessary HTML keeps Markdown portable, readable, and easier to process.